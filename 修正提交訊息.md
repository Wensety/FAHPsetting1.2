# Git 提交訊息亂碼修正指南

## 問題說明

Git 提交訊息在 GitHub 上顯示亂碼，通常是因為編碼設定不正確。

## 已執行的修正

1. 設置 Git 編碼為 UTF-8：
   ```bash
   git config --global i18n.commitencoding utf-8
   git config --global i18n.logoutputencoding utf-8
   git config --global core.quotepath false
   ```

2. 設置本地倉庫編碼：
   ```bash
   git config --local i18n.commitencoding utf-8
   git config --local i18n.logoutputencoding utf-8
   ```

## 未來提交注意事項

### 正確的提交方式

1. **使用 UTF-8 編碼提交**：
   ```bash
   git commit -m "提交訊息" --encoding=utf-8
   ```

2. **或在提交前設置環境變數**：
   ```bash
   set LANG=zh_TW.UTF-8
   git commit -m "提交訊息"
   ```

3. **使用 Git 編輯器**（推薦）：
   ```bash
   git commit
   # 在編輯器中輸入中文訊息，確保編輯器使用 UTF-8 編碼
   ```

### 修正歷史提交訊息（可選）

如果需要修正歷史提交訊息，可以使用：

```bash
# 修正最後一個提交
git commit --amend -m "正確的提交訊息" --encoding=utf-8

# 修正多個提交（需要互動式 rebase）
git rebase -i HEAD~3
# 在編輯器中將要修改的提交前的 'pick' 改為 'reword'
# 然後在每個提交時輸入正確的訊息
```

## 檔案編碼檢查

確保所有文字檔案使用 UTF-8 編碼：

- Python 檔案：在檔案開頭添加 `# -*- coding: utf-8 -*-`
- Markdown 檔案：確保以 UTF-8 編碼保存
- 其他文字檔案：使用 UTF-8 編碼

## 驗證編碼設定

檢查當前 Git 編碼設定：
```bash
git config --get i18n.commitencoding
git config --get i18n.logoutputencoding
```

應該顯示：`utf-8`

